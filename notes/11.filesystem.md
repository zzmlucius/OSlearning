## 文件系统
- 1.**文件和目录都由dirent中的name和inum指向的inode组成**。
- 2.struct dirent {string name; int inum;}
- 3.inum通过inode table指向struct dinode(disk inode)即磁盘上的数据块
- 4.struct inode是内存中的存在形式，dinode是磁盘上的。
- 5.目录和文件同构，只不过目录的inode指向的数据块存着一堆dirent
- 6.**一句话总结: dirent建立起了文件和目录从名字到真实数据的映射。**